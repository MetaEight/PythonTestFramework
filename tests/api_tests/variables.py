class Uri:
    # Get user data or create new user
    get_user = "api/users"

    # Get the page user list
    user_list = "api/users?page"

    # Register new user
    register_user = "api/register"
